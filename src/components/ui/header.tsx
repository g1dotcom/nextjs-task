// Header.tsx
const Header = ({ widthClass }: { widthClass: string }) => {
  return (
    <header className={`bg-red-500 shadow ${widthClass}`}>
      {/* Header içeriği */}dfkgjdsflkghldfjkh
    </header>
  );
};

export default Header;
